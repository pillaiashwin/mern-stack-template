const BASE_SPACING = 16;

const spacing = {
  quarter: BASE_SPACING / 4,
  half: BASE_SPACING / 2,
  default: BASE_SPACING,
  two: BASE_SPACING * 2,
  four: BASE_SPACING * 4,
};

export default spacing;
